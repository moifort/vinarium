source "https://rubygems.org"

# Used only by CI (release-ios.yml `submit` job) to attach the uploaded build to its
# App Store version and submit it for review. Not needed for local development.
gem "fastlane"
