query Search($query: String, $filters: SearchFiltersInput, $limit: Int) {
  searchBeverages(query: $query, filters: $filters, limit: $limit) {
    hits {
      matchedFields
      wine {
        ...WineListFields
      }
    }
    totalCount
  }
}
