import Foundation

/// Local-only secrets. Copy to Secrets.swift (gitignored) and fill in.
/// Only Sentry DSN remains here — auth uses Firebase ID tokens, no static
/// API token is checked in anymore.
enum Secrets {
    static let sentryDsn = ""  // Sentry DSN, leave empty to disable
}
